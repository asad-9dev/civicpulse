# Copy to backend/.env. Only needed for --summarizer claude.
# (The web app's Supabase variables live in the root .env.example.)
# (Alternatively, run `ant auth login` and leave this unset.)
ANTHROPIC_API_KEY=
